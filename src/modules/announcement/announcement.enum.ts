export enum AnnouncementStatusesEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
  AWAIT = "await",
  REJECTED = "rejected",
}

export enum AnnouncementShowMethodEnum {
  LIST = "Ads",
  MAP = "Ads_map",
}

export enum AreaUnitEnum {
  HECTARES = "hectares",
  ACRES = "acres",
  SM = "sm",
}
